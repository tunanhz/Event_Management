import { MembersView } from "@/components/organizer/members/MembersView"

/** Members ("Thành viên"): the event's team roster. */
export default function EventMembersPage() {
  return <MembersView />
}
